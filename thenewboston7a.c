#include <stdio.h>
#include <conio.h>

int main()
{
    int counter = 1;
    /* for loop: for(inital; condition; value)*/
    for(counter = 1; counter < 20; counter++){
        printf("%d\n", counter);
    }
    //getch();
    //system("cls");
    return(0);
}
